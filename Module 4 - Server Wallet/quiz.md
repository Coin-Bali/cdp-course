# Module 4: Knowledge Check

### Q1: What is the main difference between a "Server Wallet" (WaaS) and an "Embedded Wallet"?
A) Server Wallets are for developers (programmatic access); Embedded Wallets are for end-users (human access).
B) Server Wallets only hold Bitcoin.
C) Embedded Wallets have no private keys.
D) There is no difference.

### Q2: Which credential is required to SIGN a transaction with a Server Wallet?
A) Public API Key only.
B) The Project ID.
C) The `API Key Secret` (specifically the Wallet Secret portion if separated).
D) The user's password.

### Q3: You try to send a transaction and get `insufficient_funds`. What does this mean?
A) The destination address is invalid.
B) The API is down.
C) The wallet does not have enough ETH to cover the `value` + `gas_fee`.
D) The nonce is too low.

### Q4: Can a Server Wallet interact with Smart Contracts?
A) No, only simple transfers.
B) Yes, by constructing a transaction with `data` payload (function call).
C) Only on Ethereum Mainnet.

---
**Answers:**
1: A
2: C
3: C
4: B

